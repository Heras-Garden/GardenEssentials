# Testing

1. Run `/balance`, `/pay`, `/spawn`, and `/back`.
2. Test TPA with a second non-op account.
3. Run `/link` and redeem the code with Iris.
4. Confirm `/link status` and `/minecraft-link status` agree.
5. Record `/rank` in Discord, stay online and active for one XP interval, then confirm XP increased.
6. Toggle AFK and confirm the next interval gives no XP.
7. Unlink and confirm no XP is awarded.
8. Submit one bug report and one player report and confirm Iris receives each once.