# Configuration

The `iris` section controls the Minecraft side of the Discord integration.

For testing, the XP interval can be temporarily reduced. Restore the normal interval after validation.

Both GardenCore and Iris must use the same MariaDB database for account linking, reports, moderation, and XP to work.